{
  "name": "agent-deployment-challenge",
  "version": "1.0.0",
  "private": true,
  "description": "A minimal AI agent application for a deployment challenge",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others --names api,web --prefix-colors cyan,yellow \"npm run dev --workspace @challenge/api\" \"npm run dev --workspace @challenge/web\"",
    "build": "npm run build --workspace @challenge/web",
    "start": "npm run start --workspace @challenge/api",
    "test": "npm run test --workspace @challenge/api",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
